#!/bin/sh
ssid=$(nmcli -t -f active,ssid dev wifi  | grep yes | sed 's/yes://g')
if [ "$ssid" == "" ]; then
    echo " 睊 Disconnected"
    echo "#d3a04d"
else
    echo " 直 $ssid"
    echo "#f8f8f8"
fi
